networks:
  default:
    external:
      name: roboshop

services:
  # docker run -d --name mongodb --network=roboshop mongodb:1
  mongodb:
    container_name: mongodb
    image: mongodb:01v

  catalouge:
    image: catalouge:01v
    depends_on:
    - mongodb
    container_name: catalouge
    
  web:
    ports:
    - "80:80"
    image: web:01v
    container_name: web
    depends_on:
    - catalouge